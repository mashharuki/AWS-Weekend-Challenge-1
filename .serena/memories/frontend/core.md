# Frontend

- `apps/frontend`: React 19 + Vite ESM application.
- Entry: `src/main.tsx`; root component: `src/App.tsx`; global styles: `src/css/index.css`.
- Current UI is a minimal `App` component; keep browser-facing work in this module.
- Build includes TypeScript project build then Vite bundle. See `mem:suggested_commands` for module commands.